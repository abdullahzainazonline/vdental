"use client";

import { useEffect, useRef } from "react";

export default function BackgroundAnimation() {
  // Background animation disabled per request to remove floating teeth/scissors decorations.
  return null;
}
